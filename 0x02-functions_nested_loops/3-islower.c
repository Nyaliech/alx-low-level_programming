#include "main.h"

/**
 * _islower -checks for the lowercase character
 * @c: is a character argument
 * Return: 1 if c is lowercase and 0 if its otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
